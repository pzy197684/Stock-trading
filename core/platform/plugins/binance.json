{
    "name": "binance",
    "display_name": "Binance",
    "description": "Binance cryptocurrency exchange",
    "adapter_class": "core.platform.binance:BinanceExchange",
    "version": "1.0.0",
    "supported_instruments": ["SPOT", "FUTURES"],
    "capabilities": {
        "hedge_support": true,
        "position_mode": "both",
        "unit_type": "coin",
        "supported_order_types": ["market", "limit", "stop", "stop_limit"],
        "rate_limit": {
            "requests_per_second": 10,
            "orders_per_minute": 100
        }
    },
    "default_config": {
        "testnet": false,
        "recv_window": 5000,
        "timeout": 30,
        "retry_attempts": 3
    },
    "required_credentials": [
        {
            "name": "api_key",
            "display_name": "API Key",
            "description": "Binance API Key",
            "type": "string",
            "required": true
        },
        {
            "name": "api_secret",
            "display_name": "API Secret",
            "description": "Binance API Secret",
            "type": "string",
            "required": true,
            "sensitive": true
        }
    ],
    "config_schema": {
        "type": "object",
        "properties": {
            "testnet": {
                "type": "boolean",
                "default": false,
                "description": "Use testnet environment"
            },
            "recv_window": {
                "type": "integer",
                "default": 5000,
                "minimum": 1000,
                "maximum": 60000,
                "description": "Request receive window in milliseconds"
            },
            "timeout": {
                "type": "integer",
                "default": 30,
                "minimum": 5,
                "maximum": 120,
                "description": "Request timeout in seconds"
            }
        }
    },
    "metadata": {
        "author": "Stock-Trading Team",
        "created_at": "2025-09-27",
        "tags": ["crypto", "spot", "futures"],
        "documentation_url": "https://binance-docs.github.io/apidocs/"
    }
}